<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.derich.bizwiz">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/bizwiz"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bizwiz"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.Transactions"></activity>
        <activity android:name=".activities.SpinnerProducts" />
        <activity android:name=".activities.ProductsOffered" />
        <activity android:name=".activities.UpdateProducts" />
        <activity android:name=".activities.ClearDebt" />
        <activity android:name=".activities.PaidSale" />
        <activity android:name=".activities.AllClients" />
        <activity android:name=".activities.Sales" />
        <activity android:name=".activities.LoginActivity"
            android:noHistory="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ClientsDebts"
            android:screenOrientation="portrait" />
        <activity
            android:label="@string/add_debt_name"
            android:name=".activities.AddDebt"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.RegisterActivity"
            android:label="@string/register_user"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ForgotPassword"
            android:label="@string/reset_password"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ConfirmPassword"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.UsersActivity"
            android:screenOrientation="portrait" >

        </activity>
        <activity
            android:name=".activities.ClientsDetails"
            android:label="@string/add_client"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.BackupData"
            android:label="@string/add_product"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ViewClient"
            android:label="@string/clients"
            android:screenOrientation="portrait" />

        <receiver android:name=".activities.NetworkStateChecker">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>